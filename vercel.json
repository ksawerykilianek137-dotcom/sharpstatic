{
  "buildCommand": "pnpm --filter @workspace/agency-landing run build",
  "outputDirectory": "artifacts/agency-landing/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
